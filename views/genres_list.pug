extends layout 

block content 
    h1=title 
    p List of all Genres that was added till now.

    if genres.length > 0
        h3 Genres: 
        ul
        each genre in genres 
            li 
                a(href=genre.url) #{genre.name}
    else 
        h3(styel="color: red") nothing added yet!