extends layout 

block content 
    h1=user.username
    if user.picture 
        img(src=user.picture, style="width: 300px; height: 300px; border-radius: 50%")
    ul
    for song in songs 
        li 
            a(href=song.url) #{song.name}